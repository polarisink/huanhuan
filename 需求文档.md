# 欢欢需求文档

## 1. 已确认信息

- 软件名称：欢欢；应用图标使用用户提供的兔子照片。
- 软件形态：桌面软件。
- 开发技术：JavaFX。
- 目标平台：macOS 11 及以上（ARM64）、macOS 10.15.7 及以上（x86_64）和 Windows 7 SP1、10、11（x64）。最新要求恢复 Windows 7 支持。
- 输入：用户导入一个 XLSX 文件，格式参照 `files/` 中的 XLSX 样例。
- 输出：一个 XLSX 文件，原工作表保存处理后的明细，新增工作表保存科室统计。统计表仅参照现有 Word 样例的表格部分，不生成 DOCX 或报告正文。
- 当前阶段：用户已授权开始开发。

### 1.1 现有样例文件

| 文件 | 格式 |
| --- | --- |
| `files/9月时效性指标完成情况反馈01.docx` | Word（`.docx`） |
| `files/8月入院记录24小时完成（9.10）.xlsx` | Excel（`.xlsx`） |

## 2. 业务背景与目标

对入院记录、出院记录的创建时效进行计算和统计。补全缺失的记录创建时间，将超过 24 小时的记录集中展示并标红，同时生成各科室入院、出院记录的合格率报告。

## 3. 使用人员与使用场景

## 4. 功能范围

1. 导入一个符合样例格式的 XLSX 文件。
2. 使用同一行的备用记录时间补全缺失的入院、出院记录创建时间。
3. 计算入院间隔与出院间隔，单位为小时。
4. 根据两项间隔超时的数量调整数据顺序，对超时行标红。
5. 原工作表保存处理后的明细。
6. 入院指标按入院科室、出院指标按出院科室分别统计合格率，新增“科室统计”工作表，连同明细导出为一个 XLSX。

## 5. 输入文件要求

### 5.1 样例结构

输入文件仅有一个工作表，按样例列结构处理，无需多工作表选择或额外适配。现有样例的工作表名为 `ag-grid`，第 1 行是表头，数据范围为 A 至 Y 列；处理时读取唯一的工作表。

与本次处理相关的字段如下：

| 列 | 样例表头 | 用途 |
| --- | --- | --- |
| I | 出院时间 | 出院间隔的起始时间 |
| J | 入院时间 | 入院间隔的起始时间 |
| M | 出院科室 | 出院指标的统计归属科室 |
| N | 入院科室 | 入院指标的统计归属科室 |
| Q | 入院记录创建时间 | 入院间隔的结束时间，缺失时补全 |
| R | 间隔 | 写入入院间隔，单位为小时 |
| S | 出院记录创建时间 | 出院间隔的结束时间，缺失时补全 |
| T | 间隔 | 写入出院间隔，单位为小时 |
| U | 24小时入出院记录时间 | 第 1 优先级备用时间 |
| V | 24小时入院死亡记录时间 | 第 2 优先级备用时间 |
| W | 出院小结记录时间 | 第 3 优先级备用时间 |
| X | 死亡记录时间 | 第 4 优先级备用时间 |

### 5.2 时间格式

- 必须适配样例中的文本日期时间，样例采用 `年/月/日 时:分:秒`，月、日、小时可能不补零，例如 `2026/8/1 4:25:53`。
- 时间格式仅需支持样例中已有的格式，无需扩展其他时间格式。
- 所需时间无法解析时，提示原始 Excel 行号及原因，终止本次处理，详见第 9 节。

## 6. 文件处理规则

### 6.1 补全记录创建时间

对每一行独立处理 Q、S 两个字段：

1. 字段已有时间时保留原值。
2. 字段为空时，按 U → V → W → X 的顺序查找第一个非空时间，并填入该字段。
3. Q、S 同时为空时，两者按上述同一顺序分别补全，可以使用同一个备用时间。
4. 补全完成后再计算间隔，并将补全结果写入输出 XLSX。

“第一个”按上述字段优先级理解，不按时间先后挑选。Q 或 S 缺失且四个备用字段全部为空，或所选时间无法解析时，判定该行数据不合法并终止本次处理；不跳过优先选中的非法时间继续尝试低优先级时间。

### 6.2 间隔计算

| 输出列 | 计算方式 |
| --- | --- |
| R 入院间隔 | （Q 入院记录创建时间 − J 入院时间）的秒数 ÷ 3600 |
| T 出院间隔 | （S 出院记录创建时间 − I 出院时间）的秒数 ÷ 3600 |

- 1 小时按 3600 秒计算，保留不足一小时的小数部分，不按跨越的自然日期数计算，也不截断为整数小时。
- 对 R、T 重新计算，不直接使用输入文件已有的间隔值或公式。样例 R 列存在乘以 24 的小时公式，但仍须根据本次输入的时间重新计算。
- 判定超时使用未舍入的计算结果；R、T 输出保留 9 位小数。
- 负间隔保留计算所得负值，无需额外处理，按合格计入统计。

### 6.3 合格与超时判定

对于可正常判定的间隔：

- 间隔 > 24 小时：不合格，即超时。
- 间隔 ≤ 24 小时：合格；恰好 24 小时、零间隔和负间隔均属于合格。
- 入院与出院分别判定，不能用整行是否标红代替其中一项的合格结果。

补全后仍缺失时间或所需时间无法解析时，按第 9 节报错并终止本次处理，不进入合格判定或统计。

### 6.4 排序与标红

| 顺序 | 条件 | 行背景 |
| --- | --- | --- |
| 最前 | R、T 均 > 24 小时 | 整条数据行标红 |
| 其次 | R、T 中仅一项 > 24 小时 | 整条数据行标红 |
| 最后 | R、T 均 ≤ 24 小时 | 保留原有底色 |

- 按“大于 24 小时不合格”的阈值，将恰好 24 小时和负间隔归入未超时类别。
- 排序移动整行数据，保持同一条记录的字段对应关系，表头不参与排序。
- 标红覆盖数据行的全部字段，样例中为 A 至 Y 列。
- 未超时行保留原有单元格底色，排序时底色随所属记录移动；超时行按整行标红规则覆盖原底色。
- 用户允许调整同组顺序和重编序号。实现采用同组保留输入顺序，排序后将 A 列序号从 1 连续重编。
- 数据校验在排序前完成；无法计算间隔的记录触发整次处理终止，不进入排序。

### 6.5 按科室统计

- 入院统计按 N 列“入院科室”归属，出院统计按 M 列“出院科室”归属。
- M 或 N 科室名称为空时，判定该行数据不合法，报告原始 Excel 行号及具体字段，立即终止本次处理，不进行统计或导出。
- 对每一条输入记录，入院科室的入院总数增加 1，出院科室的出院总数增加 1。R > 1 时，入院科室的入院不合格数增加 1；T > 1 时，出院科室的出院不合格数增加 1。
- 入院合格率 =（该科室入院总数 − 入院不合格数）÷ 该科室入院总数 × 100%。
- 出院合格率 =（该科室出院总数 − 出院不合格数）÷ 该科室出院总数 × 100%。
- 不合格率对应为该项不合格数 ÷ 该项总数 × 100%；最终统计表继续显示样例中的合格率，即“24小时完成率”。
- 分子和分母分别按各自指标的科室归属计算，不能将同一科室的入院总数直接用作出院分母，或反向使用。
- 当同一条记录的入院科室与出院科室不同，该记录分别计入两个科室的对应指标。例如入院科室为 A、出院科室为 B 且两项均超时，则计入 A 科室的入院总数及入院不合格数，同时计入 B 科室的出院总数及出院不合格数。
- 按输入数据行直接计数，不去重，不按 Y 列“是否命中”或其他字段筛选，也不额外按统计日期筛选。
- 统计表的科室取本次入院科室与出院科室的并集，仅包含至少一项有数据的科室；两项均无数据的科室不输出，无需保留 Word 样例的科室顺序。
- 统计使用补全并重新计算后的结果，与导出 XLSX 中的明细一致。
- 所有记录通过时间和科室名称校验后才开始统计；发现不合法数据时整次处理终止，不输出部分统计。
- 某科室只有入院数据时，出院栏目显示单个反斜杠 `\`；只有出院数据时，入院栏目显示 `\`。无数据的指标不进行除法计算，也不显示为 0% 或 100%。

## 7. 输出结果要求

### 7.1 处理明细工作表

- 包含补全后的 Q、S 列时间及重新计算的 R、T 列间隔；输出 R、T 表头标注“间隔（小时）”。
- R、T 列单位为小时，保留 9 位小数。
- 按两项间隔超时数量排序，任一间隔 > 24 小时的数据行背景标红。
- 未超时行保留原有底色，底色随整行排序移动。
- 排序后 A 列序号从 1 连续重编；其余业务字段随整行保留，输出至新文件，保留用户输入原件。

### 7.2 科室统计工作表

- 在输出 XLSX 中新增“科室统计”工作表，仅参照 `files/9月时效性指标完成情况反馈01.docx` 的统计表部分。若原工作表恰好同名，新增表名追加序号，保留原表。
- 表格首行为合并标题“24小时完成率”，第二行为“科室 / 入院 / 出院”。
- 将科室名称、入院合格率、出院合格率填入对应表格栏目。
- 合格率按样例显示为百分比，保留两位小数，例如 `98.28%`。
- 科室某项没有数据时，该项显示单个反斜杠 `\`。
- 不生成样例中的报告标题、说明正文或月份和日期范围文字。
- 根据本次数据生成科室行，无数据的科室不出现，不要求沿用样例科室顺序。

### 7.3 输出文件名

- 输出一个带年月日时分秒时间戳的文件，命名为 `时效统计_yyyyMMddHHmmss.xlsx`。Java 格式中的秒使用小写 `ss`，对应用户此前写作 `yyyyMMddHHmmSS` 的时间戳要求。
- 命名示例：`时效统计_20260915143025.xlsx`。
- 遇到同名文件仍继续处理。实现采用追加递增序号避免覆盖，例如 `时效统计_20260915143025_1.xlsx`；序号被占用时继续递增。

### 7.4 输出保存位置

- 输出目录在右上角“设置”中由用户选择，默认值为当前用户的系统下载目录；主界面不展示保存目录。
- 保存设置后记住所选目录，重新启动应用后继续使用。取消设置时保留此前的目录。
- 结果文件保存到本次选定的目录，保留原始输入文件。
- 默认目录按操作系统实际下载目录解析，兼容用户重定向或自定义的下载目录。

## 8. 操作流程与界面要求

### 8.1 操作流程

用户选择 XLSX → 点击开始处理 → 校验全部记录及时间补全结果 → 计算、排序、标红及按科室统计 → 保存含两个工作表的 XLSX → 显示处理结果。

需要修改保存目录时，打开右上角“设置”，选择目录并保存。此操作独立于日常处理流程，无需每次重复设置。目录失效时提示用户到设置中重新选择。

任何数据校验失败都中止本次流程，提示原始行号，用户修正后可重新选择文件并处理。

### 8.2 界面初稿

用户授权由开发方先设计布局。初稿采用单窗口、单页布局，从上到下排列：

| 区域 | 内容与操作 |
| --- | --- |
| 页面标题 | 欢欢，显示用户提供的图片图标，副标题说明入出院记录时效统计用途 |
| 输入文件 | 显示所选 XLSX 路径，提供“选择文件”按钮 |
| 设置入口 | 右上角“设置”打开独立对话框，展示保存目录并支持选择、保存和取消；目录不在主界面展示 |
| 操作区域 | “开始处理”按钮 |
| 处理状态 | 显示校验、处理、保存等当前阶段和进度；处理期间避免重复启动 |
| 结果区域 | 成功时显示结果文件、记录数与科室数并提供“打开输出目录”；失败时显示行号与原因 |

上述为界面初稿，具体尺寸、配色和控件样式在开发阶段细化。

## 9. 异常处理与提示

### 9.1 数据不合法

以下任一情况出现时，提示“数据不合法”，指出原始 Excel 行号和具体原因，并立即终止本次处理：

- Q 或 S 中任一字段缺失，且同一行 U、V、W、X 全部为空，无法补全该字段。
- I 列出院时间或 J 列入院时间缺失。
- 计算所需的原始时间或按优先级选中的备用时间无法解析。
- M 列“出院科室”或 N 列“入院科室”名称为空。

行号使用输入文件中的实际工作表行号，包括表头占用的行，不使用 A 列序号或排序后的行号。例如：`数据不合法：第 18 行，J 列“入院时间”为空。`

科室名称为空时采用相同提示方式，例如：`数据不合法：第 18 行，N 列“入院科室”为空。`

先按输入顺序校验全部数据，发现第一条不合法记录即报错。仅在全部校验通过后执行后续计算、排序、统计与结果写出；不得跳过错误记录、输出部分结果或将其按合格处理。负间隔不属于数据不合法。

## 10. 平台兼容与运行环境

- 桌面界面使用 JavaFX。
- macOS：ARM64 目标系统为 11 及以上；x86_64 目标系统为 10.15.7 及以上。
- Windows：支持 Windows 7 SP1、Windows 10、Windows 11 的 x64 系统。
- 文件处理库由开发方选择，本项目选用 Apache POI 的 XSSF 读写 XLSX，使用 `poi-ooxml` 依赖。依据：[Apache POI 官方组件说明](https://poi.apache.org/components/index.html)。
- 技术选型：JDK 17、JavaFX 17.0.20、Apache POI 5.5.1、Maven 3.9.11（Wrapper）。macOS 使用 jpackage；Windows 使用 Launch4j 3.50（Maven 插件 2.7.0），携带 BellSoft Liberica JDK 17 Full 组装的运行时及其 JavaFX 原生模块。
- Windows 7 的最低基线为 SP1 x64，依据 [BellSoft 支持矩阵](https://bell-sw.com/pages/supported-configurations/)。不能只降低字节码版本，必须同时使用兼容的 Java、JavaFX 原生库与 EXE 启动器。实际平台验证情况见 README，不以构建脚本代替平台验证。

## 11. 性能与数据保存要求

暂不设定性能指标要求。

## 12. 安装、打包与交付要求

提供 macOS、Windows 打包脚本；仅在用户明确要求时执行打包。用户已要求生成 macOS Intel x64 版，实际交付与验证情况见 README。

| 平台 | 架构 | 交付形式 |
| --- | --- | --- |
| macOS 11 及以上 | ARM64 | DMG |
| macOS 10.15.7 及以上 | x86_64 | DMG |
| Windows 7 SP1、10、11 | x64 | 免安装的绿色版，通过 EXE 启动 |

Windows 绿色版采用包含 EXE 启动程序、依赖和运行时的文件夹，以 ZIP 分发；解压后运行 EXE。macOS DMG 包含对应架构的应用和运行时。用户无需单独安装 Java。

打包后的应用名称为“欢欢”，macOS 使用 `huanhuan.icns`，Windows 使用 `huanhuan.ico`；两个图标均由用户提供的照片转换。

## 13. 验收标准

以下检查项由已明确的功能规则推导。

| 检查项 | 预期结果 |
| --- | --- |
| 单文件处理 | 导入符合样例格式的 XLSX 后生成一个 XLSX，其中包含处理明细和科室统计两个工作表 |
| 保留已有时间 | Q、S 已有时间时不使用备用字段覆盖 |
| 按优先级补全 | 缺失 Q 或 S 时，选用同一行 U、V、W、X 中第一个非空时间 |
| 小时计算 | 相差 12 小时输出 12，相差 36 小时输出 36，相差 30 分钟输出 0.5 |
| 阈值判定 | 恰好 24 小时合格，超过 24 小时不合格；不因显示舍入改变结果 |
| 负间隔 | 保留负值并计为合格，无需额外处理 |
| 间隔精度 | R、T 输出保留 9 位小数 |
| 排序 | 两项超时排在一项超时之前，一项超时排在两项均未超时之前 |
| 序号 | 同组保留输入顺序，排序后 A 列序号从 1 连续重编 |
| 标红 | 至少一项超时的数据整行背景标红 |
| 原有底色 | 未超时行原有底色保持不变，并随所属记录排序移动；超时行整行标红 |
| 独立统计 | 一项超时不使另一项自动不合格，入院、出院分别统计 |
| 科室归属 | 入院按入院科室、出院按出院科室统计；不同科室分别获得对应项的计数 |
| 统计范围 | 按输入数据行计数，不去重、不按“是否命中”等字段筛选 |
| 科室范围 | 仅输出至少一项有数据的科室，无需保留样例科室顺序 |
| 科室单项无数据 | 无数据的栏目显示单个反斜杠 `\`，另一项正常计算 |
| 非法时间 | 所需时间无法补全、缺失或无法解析时，提示“数据不合法”、原始 Excel 行号及原因，终止本次处理且不生成结果文件 |
| 科室名称为空 | M 或 N 任一科室名称为空时，提示“数据不合法”、原始 Excel 行号及具体字段，终止本次处理且不生成结果文件 |
| 结果一致 | 科室统计工作表合格率与同一文件明细按各自科室及分母核算一致 |
| 统计表格式 | 科室统计工作表仅包含参考表格的标题、栏目和统计数据，合格率显示两位小数百分比，不含报告正文 |
| 文件命名 | 输出文件名包含年月日时分秒时间戳，Java 格式为 yyyyMMddHHmmss |
| 同名文件 | 同名时追加递增序号并继续保存，已有文件保留 |
| 输出目录 | 首次默认使用系统下载目录；可在设置中选择并保存其他目录，重启后继续沿用；主界面不显示目录 |
| 平台覆盖 | 验证 macOS 对应架构及最低目标系统上的 DMG 安装运行，以及 Windows 7 SP1、10、11 x64 上绿色版 EXE 的启动运行 |

## 14. 待确认事项

当前已提出的业务待确认事项均已确认。实际实现与验证状态见 README。
