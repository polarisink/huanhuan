# 项目协作约定

## 项目定位

- 软件名为“欢欢”，使用用户提供的兔子照片作为应用、窗口和打包图标。
- 使用 JavaFX 开发桌面软件。
- 目标平台为 macOS 11 及以上（ARM64）、macOS 10.15.7 及以上（x86_64）和 Windows 7 SP1、10、11（x64）；Windows 7 支持已恢复；根据用户在 Catalina 上的安装反馈，Intel Mac 最低版本调整为 10.15.7。
- 软件接收用户导入的 XLSX，计算入院与出院记录的时间间隔，补全缺失的记录创建时间，排序并标红超时行，输出一个 XLSX：原工作表保存处理明细，新增“科室统计”工作表保存统计结果。
- `files/` 下的 XLSX 是输入格式样例，DOCX 仅提供统计表格的格式参考；无需生成 Word 文档或报告正文。
- 需求以根目录的 `需求文档.md` 为准。

## 当前阶段

- 用户已明确开始开发，按已确认的需求实现和测试桌面应用。
- 维护 macOS、Windows 打包脚本；用户已授权 GitHub 标签推送时自动构建 Windows 10 x64 绿色版并发布 Release，手动工作流可用于验证构建。其他平台未另行要求时不执行打包。
- 未确认的需求章节保持空白，不编造功能、流程、字段、计算规则或验收标准。
- 区分用户已确认的要求、样例文件中的事实和待确认的推测；仅将已确认的要求写入需求正文。

## 文件约定

- `AGENTS.md`：开发与协作约定。
- `需求文档.md`：需求说明及后续确认结果。
- `files/`：原始样例文件，分析和开发过程中保留原件，不覆盖、重命名或删除。
- 输出目录在“设置”中选择并持久保存，默认使用当前用户的系统下载目录；主界面不显示保存目录。中间文件与原始样例分开存放。

## 后续开发原则

- 与用户沟通、编写项目说明及需求文档时使用中文。
- 用户已授权自行选择文件处理库：使用 Apache POI 的 XSSF 读写 XLSX。
- 使用 JDK 17、JavaFX 17.0.20、Apache POI 5.5.1 和 Maven Wrapper（Maven 3.9.11）。编译与测试固定使用 JDK 17，避免引入新版本 API。macOS 使用 jpackage；Windows 使用 Launch4j 3.50（Maven 插件 2.7.0）生成 EXE，内置 BellSoft Liberica JDK 17 Full 的运行时与 JavaFX 模块；不得随意换用其他发行版或 Maven 的 Windows JavaFX 原生库。打包需在目标操作系统和架构上运行。
- macOS 使用 DMG 交付；Windows 优先提供免安装的绿色版，通过 EXE 启动。
- 明确输入内容、处理规则、输出格式及异常处理方式后，再实现对应功能。
- 界面逻辑与文件处理逻辑分离；耗时文件操作不阻塞 JavaFX 界面线程。
- 避免硬编码平台路径；根据最终确定的平台范围验证文件访问与打包运行。
- 只实现已确认的需求；涉及业务含义的歧义先澄清。
- 实现后进行与改动相匹配的验证，说明实际验证结果及尚未验证的平台。

## 业务实现约定

- R 列为入院间隔，按“入院记录创建时间减入院时间”计算；T 列为出院间隔，按“出院记录创建时间减出院时间”计算。两者单位均为小时，保留不足一小时的时间差；输出 R、T 表头为“间隔（小时）”。
- 样例 R 列存在按小时计算的旧公式，不得直接沿用；根据本次输入时间重新计算 R、T 列。
- Q 或 S 列为空时，按同一行 U、V、W、X 的顺序选取第一个非空时间填入缺失字段；保留已有记录创建时间。
- 先校验全部输入记录及所需时间能否补全、解析，再执行计算、统计和导出。Q 或 S 无法补全、I 或 J 缺失或所需时间无法解析时，提示“数据不合法”，附原始 Excel 行号及原因，立即终止本次处理，不生成部分结果。
- M 列出院科室或 N 列入院科室名称为空时，同样提示“数据不合法”、原始 Excel 行号及具体字段，立即终止本次处理，不生成结果文件。
- 间隔严格大于 24 小时才属于超时；恰好 24 小时和负间隔均为合格，负间隔保留原计算结果，无需额外处理。先以完整精度判定，R、T 输出保留 9 位小数。
- 排序时移动整行数据，避免字段错位；任一间隔超时，整条数据行背景标红。
- 未超时行保留原有单元格底色，底色随整行排序移动；超时行按标红规则覆盖底色。
- 用户允许调整同组顺序和重编序号；实现采用同组保留输入顺序，排序后将 A 列序号从 1 连续重编。错误定位始终使用原始 Excel 行号。
- 入院指标按 N 列入院科室统计，出院指标按 M 列出院科室统计；同一行可分别计入两个不同科室的对应指标，分子与分母使用相同科室归属。
- 按输入数据行直接统计，不去重、不按 Y 列“是否命中”等字段筛选。
- 明细与统计工作表使用同一份处理结果，避免数据不一致。统计表输出合格率，栏目为“科室 / 入院 / 出院”。
- 统计表仅包含本次有数据的科室，无需沿用 Word 样例科室顺序或正文。
- 科室某一项总数为 0 时，该项显示单个反斜杠 `\`；有数据的另一项正常计算。
- 输入仅一个工作表；输出包含处理明细与科室统计两个工作表。时间格式支持现有样例。
- 输出文件命名为 `时效统计_yyyyMMddHHmmss.xlsx`，Java 中使用小写 `ss` 表示秒，实现用户要求的年月日时分秒时间戳。
- 同名文件不阻止处理；实现采用在文件名后追加递增序号后保存，保留已有文件。
- 主界面采用单页布局：输入文件、开始处理、进度与结果区域；右上角“设置”用于选择保存目录，详见需求文档。
- 暂无性能指标要求；当前已提出的业务待确认事项均已确认。

## 工程操作

- `./mvnw test`：运行核心自动化测试（Windows 使用 `mvnw.cmd`）。
- `./mvnw javafx:run`：运行桌面界面。
- `./mvnw -DsampleVerification=true test`：只读核验 `files/` 样例，结果写入 `target/sample-verification/`。
- `./mvnw -DuiTests=true -Dtest=MainViewSmokeTest test`：在有图形桌面的环境运行界面流程测试。
- `scripts/package-macos.sh`、`scripts/package-windows.ps1`：在对应平台打包。
- macOS 可双击根目录 `打包Mac.command` 选择版本，或给脚本传 `arm64`、`x64`、`all`；`--check` 只检查环境。Apple Silicon 上通过已有 Rosetta 运行 x64 工具链，脚本自动查找对应 JDK 17；`.tools/` 存放本机工具，不提交、不打入应用，不修改全局 Java 配置。
- 核心计算位于 `core` 包，界面位于 `app` 包，系统下载目录适配位于 `platform` 包；避免把业务判断写进界面。
- 不将原始样例或测试生成的患者明细打入安装包或提交 Git；`files/`、`target/`、`dist/`、`.tools/` 仅保留在本机。
- 图标源图位于 `packaging/icons/source.jpg`，原生图标为同目录的 ICNS、ICO，界面资源为 `src/main/resources/icons/huanhuan.png`。
- 界面字体使用 `src/main/resources/fonts/` 内的 Noto Sans CJK SC 静态常规体和粗体，通过 `AppStyles` 注册并应用；保留随附 OFL 许可，不安装系统字体。JavaFX 的 `-fx-font-family` 不支持网页 CSS 的逗号分隔字体回退列表。
